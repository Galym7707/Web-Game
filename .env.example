# Copy to .env for local development. Never commit your real .env.
# Gemini is optional — the game works fully without it.

GEMINI_API_KEY=replace_with_user_secret
GOOGLE_API_KEY=replace_with_user_secret
PORT=7860
